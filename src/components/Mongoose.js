import React from "react"

const Mongoose = (props) => {


  return(
    <div>
      {chinchillaElement}

      <button onClick={props.chinchillaToggle}>
        Abra-kadabra
      </button>

    </div>
  )
}

export default Mongoose